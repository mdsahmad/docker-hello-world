kind: pipeline
type: docker
name: hello-world

steps:
- name: docker  
  image: plugins/docker
  settings:
    # registry: 'https://registry.hub.docker.com'
    repo: shahidahmad/dronepush
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
    tags: 
      - latest
